#include <iostream>
#include <cmath>
using namespace std;
int main() {
	double a,s=1;
	int n,i;
	cin >> a >> n;
	if (n <= 0)
		cout << "Данные введены некорректно";
	else {
		for (i = 1; i <= n; i++)
			s = s + pow(a, i) * pow(-1, i);
	}
	cout << s;
}
